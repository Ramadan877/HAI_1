services:
  - type: web
    name: hai-app
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 10000
    plan: standard  # This gives you 1GB RAM instead of 512MB
    # Or use:
    # plan: premium  # This gives you 2GB RAM